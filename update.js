const fs = require('fs'); const path = require('path'); const walkSync = (dir, filelist = []) = fs.readdirSync(dir).forEach(file = filelist = fs.statSync(path.join(dir, file)).isDirectory() ? walkSync(path.join(dir, file), filelist) : filelist.concat(path.join(dir, file)); }); return filelist; }; const files = walkSync('src').filter(f =; files.forEach(file = let content = fs.readFileSync(file, 'utf8'); content = content.replace(/font-bold/g, 'font-extrabold'); content = content.replace(/bg-gradient-to-r from-white via-accent to-white/g, 'bg-gradient-to-r from-white via-[#00AEEF] to-white'); content = content.replace(/from-accent to-\[#F3E5AB\]/g, 'from-[#00AEEF] to-[#60A5FA]'); content = content.replace(/text-accent/g, 'text-transparent bg-clip-text bg-gradient-to-r from-[#00AEEF] to-[#60A5FA]'); fs.writeFileSync(file, content); });  
